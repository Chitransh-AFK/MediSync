# ============================================================
#  Smart Medicine Dispenser — Backend Environment Variables
#  Copy this file to .env and fill in your MySQL credentials.
# ============================================================

# Server
PORT=3000

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=mysql123
DB_NAME=medicine_dispenser

# CORS — set to * for demo; restrict in production
CORS_ORIGIN=*
